<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter>
    <!-- Suppress EI_EXPOSE_REP warnings - these are acceptable patterns in Bukkit plugins -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP" />
    </Match>

    <!-- Suppress EI_EXPOSE_REP2 warnings - these are acceptable patterns in Bukkit plugins -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP2" />
    </Match>

    <!-- Suppress EI_EXPOSE_STATIC_REP2 warnings - plugin singleton pattern is standard in Bukkit -->
    <Match>
        <Bug pattern="EI_EXPOSE_STATIC_REP2" />
    </Match>

    <!-- Suppress null pointer warnings for getCommand() in onEnable — commands are guaranteed
         to be present if declared in plugin.yml -->
    <Match>
        <Class name="com.shweit.expendablebackpacks.ExpendableBackpacks" />
        <Method name="onEnable" />
        <Bug pattern="NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE" />
    </Match>

    <!-- Suppress null pointer warning for getBackpackUUID — callers are expected to check
         isBackpack() before calling this method -->
    <Match>
        <Class name="com.shweit.expendablebackpacks.items.BackpackItem" />
        <Method name="getBackpackUUID" />
        <Bug pattern="NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE" />
    </Match>

    <!-- Suppress null pointer and redundant nullcheck warnings for BackpackCraftingListener.onCraft -->
    <!-- SpotBugs has conflicting warnings about getMatrix() nullability -->
    <Match>
        <Class name="com.shweit.expendablebackpacks.listeners.BackpackCraftingListener" />
        <Method name="onCraft" />
        <Or>
            <Bug pattern="NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE" />
            <Bug pattern="RCN_REDUNDANT_NULLCHECK_OF_NONNULL_VALUE" />
        </Or>
    </Match>
</FindBugsFilter>
